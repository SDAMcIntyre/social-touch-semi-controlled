"""Serialisation for tactile-quantification artifacts.

Deliberately empty of re-exports: the modules beneath are imported by their
full path so this package never pulls a serialisation dependency into a caller
that does not need one.
"""
